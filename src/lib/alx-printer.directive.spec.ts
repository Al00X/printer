import { AlxPrintDirective } from './alx-print.directive';

describe('AlxPrintDirective', () => {
  it('should create an instance', () => {
    const directive = new AlxPrintDirective();
    expect(directive).toBeTruthy();
  });
});
